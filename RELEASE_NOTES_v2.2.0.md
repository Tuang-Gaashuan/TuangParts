# 元器件仓库 v2.2.0

本版本完善远端事件同步、读取幂等保护、账本审计和 Windows 发布包。

## 更新内容

### 远端事件同步

- 严格区分 GitHub/Git 与 Gitee 配置，当前平台和远端地址不匹配时拒绝操作。
- 推送时直接使用当前配置的完整远端地址，避免本地 `origin` 配置串台。
- 程序内部 Git 提交隔离用户全局 GPG 签名设置，不再因本地签名 PIN 输入中断同步。
- 同步检查只检测远端，不提前消耗未读事件。
- 读取事件时按 `event_id` 记录读取状态，重复读取不会重复写入本机账本。
- 本机账本保存远端 `event_id`，即使读取状态被重置，也能阻止同一事件重复入账。
- 远端同步页面区分“读取远端事件”和“提交本机账本”两个操作。

### 账本与撤回

- 一次业务操作对应一条账本主记录和一个撤回快照。
- 跨子分类 BOM 取出使用单次事务，不拆成多条主记录。
- 支持整笔、明细级和批量撤回，以及取消撤回。
- 保留原始业务记录、撤回快照和审计事件，不物理删除历史事实。
- 线上同步记录只读，不参与本机库存撤回。

### 发布包

- `dist/parts-warehouse/parts-warehouse.exe`：目录版，适合完整部署和快速启动。
- `share_dist/parts-warehouse.exe`：单文件分享版，不携带本机库存、同步地址或 API Key。
- `parts-warehouse-v2026.08.28-full.zip`：完整目录部署包，包含目录版程序及当前数据。
- 源码发布母本：只包含源码、文档、测试和打包配置，用户数据与构建缓存不入库。

## 验证情况

- Python 编译检查通过。
- JavaScript 语法检查通过。
- `git diff --check` 通过（仅有 Windows 换行提示）。
- 目录版 EXE 实际启动，首页 HTTP 返回成功。
- 单文件版 EXE 实际启动，首页 HTTP 返回成功。
- 远端 Gitee 事件读取、重复读取和账本幂等行为已实际验证。
- 当前 Python 环境未安装 `pytest`，自动化测试未执行。

## 数据安全

发布种子设置不包含 API Key、本机路径或远端仓库地址。目录版完整 ZIP 含当前正式数据，请勿将其作为公开分享包；公开分享应使用单文件版或源码包。
