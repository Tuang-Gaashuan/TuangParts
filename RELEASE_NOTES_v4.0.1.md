# parts-warehouse v4.0.1 发布说明

> 发布日期：2026-08-29
> 说明：v4.0.0 上传完毕后，立即发现并修复了一个库存数据 bug，因此发布 4.0.1。

## 修复内容：0 数量元器件不允许存在

**问题**：库存中出现了 357 条数量为 0 的元器件记录。

**根因**：历史「数据包导入」流程存在列错位——撤回日志快照（列序为
`[子分类, 原行号, 名称, 品牌, 封装, 数量, ...]`）被当作普通库存行解析时，
数量列被错读或丢失，恢复进库存后全部变成 0 数量；部分行甚至把数量挤进了
品牌列（`brand='10'`）。

**数据修复**（本机，清理前已完整备份）：
- 59 条数量被挤进品牌列 → 恢复正确数量
- 297 条为重复行（同子分类下已有同名同品牌且数量正常的行）→ 删除
- 1 条唯一记录 → 从撤回日志快照找回数量
- 清理后：0 数量行 = 0，库存全部为正数

**代码防线**（今后任何路径不再产生 0 数量）：
- 新增 `sanitize_qty()` + `clean_rows()`，所有写盘路径（导入、取出、保存、
  撤回、合并）统一过滤数量 ≤ 0 / 空 / 非数字的行
- 撤回机制增强：取出归零后行被删，撤回时按快照名称匹配重建行，数量正确恢复
- 表格保存前校验数量必须 > 0，误改 0 时明确提示

## 验证

- 30 项自动化测试全部通过
- 端到端验证：录入 → 全量取出 → 行消失 → 撤回 → 行重建数量正确
- 手动编辑数量为 0 / 空 → 后端过滤 + 前端拦截

## 安装

- 目录版：解压 `parts-warehouse-4.0.1-windows.zip` 中 `directory/`，双击 `parts-warehouse.exe`
- 单文件版：`parts-warehouse-4.0.1-windows.zip` 中 `onefile/parts-warehouse.exe`
- 依赖 Windows 10/11 自带 WebView2

## 完整功能

同 v4.0.0（见《使用说明书》与《更新介绍-4.0.0》），并包含本版本修复。
