# parts-warehouse v4.0.0 发布说明

> 发布日期：2026-08-29

## 亮点

1. **全新 Codex 风格工作区界面**
   - 深色中性、高信息密度布局，固定左侧导航，细边框、低圆角、无渐变
   - 侧栏支持展开/收起、拖拽调宽、状态记忆、移动端抽屉
   - 主页改为信息工作区：分类总览 + 近期库存 + 最近出入，真实数据渲染

2. **全库搜索（Ctrl+K / Cmd+K）**
   - 独立搜索工作区，无需先进入库存分类
   - 搜索名称/型号、品牌、封装、库位、规格、一级分类、子分类
   - 结果直达对应子分类表格

3. **数据包导入分类修复（重要）**
   - 旧版导出包携带「撤回日志」快照导致导入错位、批量落入未分类的问题已修复
   - 导出不再打包撤回日志；导入跳过 undo-* 文件（兼容旧包）
   - 优先按 ZIP 一级分类目录 + 文件名恢复，目录不匹配时逐行读取行内「子分类」恢复
   - 764 条历史未分类元件全部按原分类归位，未分类清零，零丢失

4. **深色主题与背景透明度修复**
   - 修复 WebView2 首帧「背景深色但侧栏/窗口雪白」
   - 启用背景图后卡片透明度滑块真实生效

5. **新图标**：EXE / 窗口 / favicon 三处统一更换

## 完整功能

- 库存管理：总览、分类、子分类表格（编辑/排序/合计）、低库存预警、未分类区
- 存入：手动 / AI 填入 / 批量导入（文本/Excel，AI+纯规则双引擎）/ OCR 拍照入库
- 取出：手动取出 / BOM 匹配取出（四色状态）/ 按生产份数放大
- BOM：BOM 匹配与 BOM 清单两个独立业务，清单 Excel 化持久化
- 账本与撤回：一次操作一笔记录，整笔/单项/取消撤回
- 品牌库：按品牌聚合、多写法合并、品牌库档案生成
- 线上同步：Gitee/GitHub 双平台，账本一键提交线上
- 数据包：ZIP 导出/导入、自动备份、迁移分享
- 外观：5 套主题、背景图（亮度/透明度/填充）、字号、字体、背景深色强度

## 安装

- 目录版：解压 `parts-warehouse-4.0.0-windows.zip` 中 `directory/`，双击 `parts-warehouse.exe`
- 单文件版：`parts-warehouse-4.0.0-windows.zip` 中 `onefile/parts-warehouse.exe`
- 依赖 Windows 10/11 自带 WebView2

## 校验

SHA256 见 `SHA256SUMS.txt`（随 Release 资产）。

## 使用说明

完整《使用说明书》与《更新介绍》见源码目录 `parts-warehouse-src-4.0.0/`：
- `使用说明书.md` —— 全部功能、分类体系、设置详解、更新记录
- `更新介绍-4.0.0.md` —— 本次更新明细
