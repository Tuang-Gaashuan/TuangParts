# TuangParts v4.0.2 发布说明

> 发布日期：2026-08-31

## 本次更新

- 增加“关于”页：作者 Tuang、软件说明、持续改进提示与 GitHub 正式版本检测集中在此处。
- 版本检测固定查询 `Tuang-Gaashuan/TuangParts` 的公开 GitHub Releases，不依赖库存线上同步配置，也不需要 Token。
- 库存详情操作改为右上角可展开操作条，不再挤压库存表格；返回操作收纳到操作条底部，且只在库存分类详情显示。
- 修复品牌库、全库搜索工作区及搜索结果直达库存分类。
- 数据包现在独立处理 BOM 清单：导出时原样包含，导入时按清单 ID 去重恢复；不会被当成库存或未分类数据。
- 批量导入、存入、取出与 BOM 取出均写入账本，记录库存变化前后数量。
- 工程目录已整理：维护脚本归入 `tools/maintenance/`，文档归入 `docs/`，构建与历史产物归入 `artifacts/`。

## 数据与升级

推荐升级方式：在旧版本“设置 → 数据管理”导出数据包，下载并解压新版到新的独立目录，再在新版导入数据包。

数据包迁移库存、未分类和 BOM 清单；操作账本不随数据包迁移，保留在原本地目录中。发布包只带干净种子数据，不含任何库存、BOM 清单、账本、背景图片、API Key 或线上同步地址。

## 发布文件

- `parts-warehouse-4.0.2.exe`：单文件版。首次启动会在 EXE 旁创建 `data/`。
- `parts-warehouse-4.0.2-windows.zip`：完整 Windows 包，含 `directory/` 目录版和 `onefile/` 单文件版。

推荐使用 ZIP 中的目录版：`directory/parts-warehouse/parts-warehouse.exe`。

## 验证

- Python 源码编译检查通过。
- 前端 JavaScript 语法检查通过。
- 目录版与单文件版均从最终交付位置实际启动，首页与版本检查 API 返回正常。
- 发布 ZIP 已检查，不含个人运行数据、账本、背景、撤回记录、导出包或凭据。
