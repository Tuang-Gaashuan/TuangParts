# 元器件仓库 v3.0.0

v3.0.0 将库存业务记录、撤回审计和 Git 事件同步整理为可独立验证的发布版本，同时补齐首页与业务页的视觉语义。

## 更新内容

### 出入账本与撤回审计

- 每次入库、取出、BOM 取出等业务操作保存为一条主账本记录和可追溯的撤回快照。
- 跨分类 BOM 取出使用单次事务，避免同一次操作拆成多条主记录。
- 支持整笔撤回、明细撤回、批量撤回和取消撤回，历史事实保留用于审计。
- 远端读取的记录保持只读，不参与本机库存撤回。

### 线上同步

- 明确区分 GitHub/Git 与 Gitee 的平台配置；平台与远端地址不匹配时拒绝同步。
- 推送使用当前页面配置的完整远端地址，避免复用本地 `origin` 时串到错误平台。
- 程序内 Git 提交关闭 GPG 签名，避免全局 GPG/PIN 交互中断同步。
- 远端事件以 `event_id` 实现读取幂等保护，同一事件不会重复写入本机账本。
- 同步页面独立提供“读取远端事件”和“提交本机账本”，已经提交的记录可以作为新的同步事件再次提交。

### 界面

- 首页“出入账本”使用红色审计色卡，页面内标题、工具栏和表头保持同一语义。
- 首页“线上同步”使用青蓝网络色卡，页面内同步操作区域保持同一语义。
- 深色主题同步提供深红和深青适配，保持卡片层级与对比度。
- 移除旧的 TP 图标与 favicon 残留。

## 发布包

- `parts-warehouse.exe`：单文件分享版。首次运行在 EXE 旁生成干净数据目录，不含库存、API Key、远端地址或本机路径。
- `parts-warehouse-full.zip`：完整目录运行环境。解压后直接运行，包含所有依赖和相同的干净种子数据。
- `parts-warehouse-src/`：源码、测试、打包配置与文档；不包含二进制、构建缓存、用户数据或凭据。

## 验证

- Python 编译检查通过。
- JavaScript 语法检查通过。
- `git diff --check` 通过。
- 目录版 EXE 已实际启动，首页返回当前账本与同步页面标识。
- 单文件 EXE 已实际启动；首次生成的数据目录已验证为离线、无 API Key、无远端配置、无导出 ZIP 的干净种子。

## 数据安全

上传目录中的两个发布包均不包含个人库存数据、API Key、远端地址或本机路径。开发目录中的数据和独立迁移包不属于本次上传内容。
