# 元器件仓库 v2.1.0

本版本重点完善 Excel/BOM 导入流程，并优化按份数取出体验。

## 更新内容

### Excel / BOM 导入

- 支持 `.xlsx` 与旧版 `.xls` 文件读取。
- 自动识别前置标题、说明行、空行之后的真实表头，不再依赖固定表头位置。
- 多工作表文件会自动选择最像 BOM/采购明细的工作表。
- 扩展常见中英文列名识别，包括型号、Value、Reference、Footprint、Package、Manufacturer、Quantity、Description、规格参数等。
- 数量格式统一处理，例如 `2 个`、`3.0`、`2,000` 可正常参与后续计算。
- 电容规格参数不再丢失，容值、耐压、材质等信息会保留在名称和规格字段中。
- 规则解析上传 Excel 与 AI 解析上传 Excel 统一使用同一套增强读取模型。

### BOM 按份数取出

- BOM 文件解析完成后，在导入区域显示固定的“取出几份”控件。
- 支持反复修改份数并重新匹配库存。
- 份数会实际乘到每条 BOM 需求数量后再进行库存匹配。
- 保留精确匹配、相似替换料、不足和缺料状态显示。

### 发布与数据安全

- 主仓库只保留源码、文档和必要的测试文件。
- `parts-warehouse.exe` 为独立单文件分享版，不携带个人库存数据或 API Key。
- `parts-warehouse-full.zip` 为完整目录部署包，包含程序依赖和目录版当前数据。
- 用户数据仍保存在程序目录旁的 `data\\` 中，升级前建议先备份该目录。

## 验证情况

- Python 单元测试：11 项全部通过。
- Python、JavaScript 语法检查通过。
- 目录版 EXE 启动冒烟测试通过，首页 HTTP 200。
- 单文件版 EXE 启动冒烟测试通过，首页 HTTP 200。

## 下载

- 单文件版：`parts-warehouse.exe`
- 完整版：`parts-warehouse-full.zip`
- 源码：本仓库中的 `parts-warehouse-src/`
